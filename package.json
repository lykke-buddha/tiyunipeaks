{
    "name": "tiyuni-peaks-root",
    "private": true,
    "scripts": {
        "build": "cd website && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}